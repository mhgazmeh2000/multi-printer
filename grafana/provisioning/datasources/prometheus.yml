# Grafana datasource provisioning
# Copy this to /etc/grafana/provisioning/datasources/ or your Grafana data dir
# Adjust 'url' to your printer server address

apiVersion: 1

datasources:
  - name: Printer-Metrics
    type: prometheus
    access: proxy
    url: http://localhost:5050
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: "30s"
